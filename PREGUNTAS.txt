PREGUNTAS
=========

- singleton lleva destructores? (no hay patrones en el teorico con destructores)
- ponemos segundos para la hora
- se puede tener un Dictionary/Collection de DataValues?
- colecciones usan las interfaces o tenemos que usar list directamente?
- MamejadorOfertaLaboral::getDataOFertaLaboral (datas) va con dictionary o collection?
- si retornamos un List, es un ICollection? O tenemos que hacer un cast? 
- cambios en el patron strategy?
- en Tiempo, esta bien? Usar segs?
- en el getInstance de un singleton, se pueden pasar parametros (ej: para inicializar un atributo - set), o se pueden usar geters/seters
- hay que usar los int y string que nos proveen? (para las colecciones - IKey)
- asociar Inscripcion - Estudiante
- cuando incluimos un ICollection, tenemos que incluir tambien el .h del tipo que va en la coleccion?
- 
